<source>
  @type forward

  # fluentd listens on this tcp port:
  port 24224
</source>

<match fluentd.test.**>
 @type file
 path ./fluent-test2b
 compress gzip
 <buffer>
#  flush_mode immediate
   flush_mode interval
   flush_interval 60s
 </buffer>
</match>
